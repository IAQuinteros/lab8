 <div class="container mt-4">
 <div class="card text-bg-light mb-3 mx-auto" style="max-width: 28rem;">
  <div class="card-header">Crear Mensaje</div>
  <div class="card-body">
    <p class="card-text">
<%= form_with(model: @message, local:true) do |f| %>
<div>
    <p>
        <strong><%= f.label :chat_id, "Chat asociado" %></strong> <br>
        <%= f.select :chat_id, Chat.includes(:sender, :receiver).map { |chat| ["chat ##{chat.id} (#{chat.sender.email} → #{chat.receiver.email})", chat.id]}, {}, {id: "chat-select"} %>
    </p>

        <p>
        <strong><%= f.label :body, "Mensaje" %> </strong><br>
        <%= f.text_area :body %>
    </p>

    <%= f.submit "Crear mensaje", class: "btn btn-primary btn-sm" %>
    <%= link_to 'Volver', messages_path, class: "btn btn-primary btn-sm" %>
</div>
<% end %>
</p>
</div>
<div>
